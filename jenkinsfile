pipeline {
	agent {
    	stages {
        	stage('Build') {
            	
               		echo 'build success'
            	
        	}
        	stage('Test') { 
            	
               		echo  'test success'
            	
      		}
    	}
    }
}
