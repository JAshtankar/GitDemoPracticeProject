pipeline {
	agent {
    	stages {
        	stage('Build') {
            	steps {
               		echo 'build success'
            	}
        	}
        	stage('Test') { 
            	steps {
               		echo  'test success'
            	}
      		}
    	}
    }
}
